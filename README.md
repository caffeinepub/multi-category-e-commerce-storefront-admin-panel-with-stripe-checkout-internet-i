## Multi-category e-commerce storefront + admin panel with Stripe checkout, Internet Identity login, wishlist, and order tracking

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
